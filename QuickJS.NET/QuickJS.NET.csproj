<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>net8.0</TargetFrameworks>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <RootNamespace>QuickJS</RootNamespace>
    <Platforms>AnyCPU</Platforms>
    <RuntimeIdentifiers>win-x64;linux-x64</RuntimeIdentifiers>
    <AssemblyOriginatorKeyFile>QuickJS.snk</AssemblyOriginatorKeyFile>
    <SignAssembly Condition="Exists($(AssemblyOriginatorKeyFile))">true</SignAssembly>
    <Configurations>Debug;Release;Nuget_Release</Configurations>
  </PropertyGroup>

  <PropertyGroup>
    <Version>0.0.4.0</Version>
    <Description>C# bindings for QuickJS, a small JavaScript engine by Fabrice Bellard and Charlie Gordon.</Description>
    <PackageTags>QuickJS, JavaScript, ECMAScript, interpreter</PackageTags>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <Authors></Authors>
    <PackageProjectUrl>https://github.com/fallenwood/quickjs.net</PackageProjectUrl>
    <RepositoryType>git</RepositoryType>
    <RepositoryUrl>https://github.com/fallenwood/quickjs.net</RepositoryUrl>
    <NeutralLanguage>en</NeutralLanguage>
    <PackageReleaseNotes>
      QuickJS 2024-10-15
    </PackageReleaseNotes>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)'=='Nuget_Release'">
    <DebugType>portable</DebugType>
    <DebugSymbols>true</DebugSymbols>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <IncludeSymbols>true</IncludeSymbols>
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>
  </PropertyGroup>

  <ItemGroup>
    <None Include="runtimes/win-x64/quickjs.dll">
      <Pack>true</Pack>
      <PackagePath>runtimes/win-x64</PackagePath>
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
    <None Include="runtimes/linux-x64/libquickjs.so">
      <Pack>true</Pack>
      <PackagePath>runtimes/linux-x64</PackagePath>
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
    <Content Include="QuickJS.NET.props">
      <PackagePath>build/QuickJS.NET.props</PackagePath>
    </Content>
  </ItemGroup>
</Project>
